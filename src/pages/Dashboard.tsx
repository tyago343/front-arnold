import AuthenticatedLayout from "../layouts/authenticated.layout";

const Dashboard = () => {
  return (
    <AuthenticatedLayout>
      Aca va a ir el contenido del dashboard
    </AuthenticatedLayout>
  );
};

export default Dashboard;
